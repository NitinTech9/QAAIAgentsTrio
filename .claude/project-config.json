{
  "$schema": "schemas/project-config.schema.json",
  "project": {
    "name": "YourProject",
    "dbVerification": true,
    "jira": {
      "cloudId": "your-org.atlassian.net",
      "testIssueType": "Test",
      "issueLinkType": "Test",
      "batchSize": 8,
      "testAssigneeAccountId": null
    },
    "testFramework": "cypress",
    "paths": {
      "ticketContext": "docs/.ticket-context",
      "manualCases": "docs/test-cases",
      "apiTests": "cypress/e2e/API",
      "uiTests": "cypress/e2e/UI",
      "jiraTicketTests": "cypress/e2e/JiraTicket",
      "pages": "cypress/e2e/pages",
      "support": "cypress/support/commands.js",
      "dataFactory": "cypress/support/dataFactory.js",
      "tasks": "cypress/tasks",
      "fixtures": "cypress/fixtures",
      "knowledge": "cypress/knowledge",
      "swaggerPrimary": "cypress/fixtures/swagger.json",
      "swaggerSecondary": null,
      "namingConventions": null,
      "logs": "cypress/logs",
      "reports": "cypress/reports",
      "screenshots": "cypress/screenshots"
    },
    "auth": {
      "_comment": "primary = your main app under test. If your suite tests a second backend, replace `secondary: null` with a block of the same shape (loginCommand, sessionCookieAlias, csrfTokenAlias). The flattened keys below are the default when a spec doesn't say which app it targets.",
      "primary": {
        "loginCommand": "cy.loginAndGetSessionCookie()",
        "sessionCookieAlias": "@sessionCookie",
        "csrfTokenAlias": "@csrfToken"
      },
      "secondary": null,
      "loginCommand": "cy.loginAndGetSessionCookie()",
      "sessionCookieAlias": "@sessionCookie",
      "csrfTokenAlias": "@csrfToken"
    },
    "app": {
      "_comment": "Used by explore-live-app.md for browser-driven exploration. baseUrls should mirror the environments in your test-runner config; credentials are read from envFile, never hardcoded. Set secondaryBaseUrl only if you test a second backend.",
      "primaryBaseUrl": "http://localhost:4000",
      "secondaryBaseUrl": null,
      "loginPath": "/login",
      "envFile": "cypress.env.json",
      "emailKey": "LOGIN_EMAIL",
      "passwordKey": "LOGIN_PASSWORD"
    },
    "testLimits": {
      "_comment": "Per SPEC FILE targets — multi-layer tickets get multiple spec files instead of dropped coverage (see create-ui-automated-test-cases.md)",
      "bugMaxTests": 2,
      "storyMaxTests": 8
    },
    "runCommand": {
      "_comment": "{specFile} and {env} are replaced at runtime by run-tests.md. Add any environment prefix your machine needs (e.g. a Node version manager) to all three commands.",
      "headed": "npx cypress run --spec \"{specFile}\" --headed --reporter mochawesome",
      "headless": "npx cypress run --spec \"{specFile}\" --reporter mochawesome",
      "withEnv": "CYPRESS_ENV={env} npx cypress run --spec \"{specFile}\" --reporter mochawesome"
    },
    "runTimeoutMs": 600000,
    "productCode": {
      "_comment": "rootPaths MUST be overridden in .claude/project-config.local.json with your local repo paths",
      "rootPaths": [],
      "sourceGlobs": ["*.go", "*.js", "*.jsx", "*.ts", "*.tsx", "*.sql"],
      "excludeDirs": ["node_modules", ".git", "dist", "build", "coverage", "vendor"]
    },
    "postman": {
      "_comment": "Describe your app's real auth flow here so generated collections model it correctly (cookie vs bearer, where the CSRF token comes from). See cypress/support/commands.js in your repo for the source of truth.",
      "collectionsPath": "postman/collections",
      "authType": "cookie",
      "loginEndpoint": "/session",
      "csrfEndpoint": "/api/session"
    }
  }
}
