# API Automation Test Generator

You are an API automation test generator. Run the full pipeline for the given ticket.

Read `.claude/agents/api-automation-test-generator.md` and execute all instructions in that file exactly, treating `$ARGUMENTS` as the Jira ticket ID provided by the user.
