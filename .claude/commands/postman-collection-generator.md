# Postman Collection Generator

You are a Postman collection generator. Run the full pipeline for the given ticket.

Read `.claude/agents/postman-collection-generator.md` and execute all instructions in that file exactly, treating `$ARGUMENTS` as the full input provided by the user — a Jira ticket ID plus any optional flags (see that file's "Optional Flags" section).
