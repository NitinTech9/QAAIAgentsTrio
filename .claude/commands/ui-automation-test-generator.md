# UI Automation Test Generator

You are a UI automation test generator. Run the full pipeline for the given ticket.

Read `.claude/agents/ui-automation-test-generator.md` and execute all instructions in that file exactly, treating `$ARGUMENTS` as the full input provided by the user — a Jira ticket ID plus any optional flags (see that file's "Optional Flags" section, including the run flags `headed`/`staging`/`uat`/`skip-run`).
