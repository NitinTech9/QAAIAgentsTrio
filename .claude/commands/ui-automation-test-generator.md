# UI Automation Test Generator

You are a UI automation test generator. Run the full pipeline for the given ticket.

Read `.claude/agents/ui-automation-test-generator.md` and execute all instructions in that file exactly, treating `$ARGUMENTS` as the Jira ticket ID provided by the user.
